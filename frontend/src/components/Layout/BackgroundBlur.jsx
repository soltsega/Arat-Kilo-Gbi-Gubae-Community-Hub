/**
 * Animated background blur component — matches the original radial gradient
 * with noise overlay and floating blur elements.
 */
export default function BackgroundBlur() {
  return <div className="background-blur"></div>;
}
